<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.OrderDetailMapper">

    <insert id="insertBatch">
        INSERT INTO sky_take_out.order_detail (name, image, order_id, dish_id, setmeal_id, dish_flavor, amount)
        VALUES
        <foreach collection="orderDetails" item="item" separator=",">
            (#{item.name}, #{item.image}, #{item.orderId}, #{item.dishId}, #{item.setmealId}, #{item.dishFlavor}, #{item.amount})
        </foreach>
    </insert>
    <select id="selectNameByOrderIds" resultType="com.sky.dto.OrderDetailNameDTO">
        SELECT od.order_id, GROUP_CONCAT(od.name) AS name
        FROM sky_take_out.order_detail od
        WHERE od.order_id IN
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
        GROUP BY od.order_id
    </select>


</mapper>